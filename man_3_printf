.TH printf 1 "November 2024" "Linux Manual"

.SH NAME
printf - A program demonstrating the usage of our printf function in C.

.SH SYNOPSIS
.I "FORMAT [ARGUMENTS]..."

.SH DESCRIPTION
The
.B printf
command formats and prints its arguments.
.PP
It uses a format string to specify how to print each argument and
works with integers, percentages, strings, and characters.

.SH OPTIONS
TP
.B %s
Prints a string.
.TP
.B %d
Prints a decimal integer.
.B %c
Prints a character.
.B %i
Prints an integer (matching with decimals, octal & hexadecimals).
.B %%
Prints a percentage.

.SH EXAMPLES
.PP
Print a greeting message:
.B
printf "Thank %s!\n" "You"

.SH BUGS
If there are any bugs in your program, or anything that does 
not work the way you want it to, put it here so the user knows.

.SH AUTHOR
Written by Ephely Koke & Jules Tertre.

